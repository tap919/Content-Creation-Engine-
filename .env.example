# Agentic Content Factory
# Environment Configuration Template
#
# Copy this file to `.env` and REPLACE ALL PLACEHOLDER VALUES before running the application.
# WARNING: None of the keys in this file are real. If you leave any placeholder (e.g. `your_openai_api_key_here`)
#          the application may fail at runtime or behave unexpectedly.

# OpenAI API Key (required for LLM brief generation)
# NOTE: Replace `your_openai_api_key_here` with a real key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Twitter/X API Credentials (for trend scraping)
# NOTE: Replace these with real credentials from the Twitter/X Developer Portal (https://developer.x.com/)
TWITTER_API_KEY=your_twitter_api_key
TWITTER_API_SECRET=your_twitter_api_secret
TWITTER_BEARER_TOKEN=your_twitter_bearer_token

# Instagram API (for deployment)
# NOTE: Replace with a real access token obtained from your Meta/Instagram developer app.
INSTAGRAM_ACCESS_TOKEN=your_instagram_token

# TikTok API (for deployment)
# NOTE: Replace with real TikTok client credentials from the TikTok for Developers console.
TIKTOK_CLIENT_KEY=your_tiktok_client_key
TIKTOK_CLIENT_SECRET=your_tiktok_client_secret

# YouTube API (for deployment)
# NOTE: Replace with a real YouTube Data API key from Google Cloud Console (APIs & Services -> Credentials).
YOUTUBE_API_KEY=your_youtube_api_key

# Redis Configuration (for caching and task queue)
# You can keep this default for local development, or point to your own Redis instance.
REDIS_URL=redis://localhost:6379/0

# Database Configuration
# Default: local SQLite file for development. Change to your production database URL.
DATABASE_URL=sqlite:///./content_factory.db

# PostgreSQL Configuration (for docker-compose)
# NOTE: Set a strong password for production deployments!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=content_factory

# n8n Configuration (optional, for workflow automation)
# NOTE: Set strong credentials if enabling n8n with --profile automation
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme_in_production

# Logging
LOG_LEVEL=INFO

# Environment
ENVIRONMENT=development
